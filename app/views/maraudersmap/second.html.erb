<h1>Box Marauder's Map: Second Floor</h1>
<%= render "nav" %>
<p>

<div id="map_container">
	<img class="map" src="/images/second.png"/>
	<% @second_floor_users.each do |hub, users| %>
		<div id="hub2-<%= hub %>" class="hub">
			<% users.each do |user| %>
				<%= user %><br>
			<% end %>
		</div>
	<% end %>
</div>
